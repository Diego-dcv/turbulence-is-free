# Datos

Esta carpeta contendrá los CSV de resultados generados por el script `codigo/experimento_pcc.py`.

Para generar los datos:
```bash
cd codigo
python experimento_pcc.py
```

Los resultados se guardarán automáticamente en `datos/resultados_pcc.csv`.
